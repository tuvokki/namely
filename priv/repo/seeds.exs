defmodule Namely.Data do
  alias Namely.Name

  Name.create("destroy", "Remove a record leaving no trace")
  Name.create("save", "Persisting a record somewhere.")
  Name.create("delete", "Remove a record")
end
